{
  "name": "@aeonic/marketing",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "nuxt dev --host 127.0.0.1",
    "build": "nuxt generate && node scripts/copy-static-output.mjs",
    "generate": "nuxt generate",
    "generate:netlify": "npm run build",
    "preview": "nuxt preview --host 127.0.0.1"
  },
  "dependencies": {
    "nuxt": "^3.17.5",
    "vue": "^3.5.16"
  },
  "devDependencies": {}
}
